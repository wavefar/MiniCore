<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        tools:context=".activity.GoodsDetailActivity">

        <include layout="@layout/head_layout" />

        <org.wavefar.lib.ui.widget.BottomNavigationViewEx
            android:id="@+id/goods_detail_navigation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/color_white"
            app:itemIconTint="@color/bottom_tab_text_color"
            app:itemTextColor="@color/bottom_tab_text_color"
            app:menu="@menu/goods_detail_top_menu" />

        <FrameLayout
            android:id="@+id/goods_detail_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    </LinearLayout>
</layout>
